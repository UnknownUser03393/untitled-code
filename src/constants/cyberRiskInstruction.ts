/**
 * CYBER_RISK_INSTRUCTION — removed in this build.
 *
 * Anthropic Safeguards content-policy copy that told the model to refuse
 * certain security requests (dual-use tools, pentesting, CTF, etc.).
 * Irrelevant and counterproductive for third-party-gateway users. Kept as an
 * empty export so existing imports don't break.
 */
export const CYBER_RISK_INSTRUCTION = ''
